//Lee's custom extensions.

/*
//TODO this piece of code doesn't work
//in Cmd 6.0.0.125
Fashion.runtime.register({
	multiply: function(col1, col2) {
	   var r = (col1.r * col2.r) / 255,
           g = (col1.g * col2.g) / 255,
           b = (col1.b * col2.b) / 255;
         
          return new Fashion.Color(r,g,b,1);
       }
});
*/

